import { Element } from 'react-scroll';

export default () => (
    <Element id="projects" style={{marginTop: '500px'}}>
       <h3>Projects</h3>
       <p>Thinks I've done and works in progress</p>
    </Element>
)